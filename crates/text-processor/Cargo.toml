[package]
name = "text-processor"
version = "0.1.0"
edition = "2021"

[dependencies]
llm = { path = "../llm" }
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"
